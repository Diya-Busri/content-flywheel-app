# FFmpeg package worker (generated)

These are copied from `node_modules/@ffmpeg/ffmpeg/dist/esm` by `npm install` so the browser runs an **unbundled** module worker. Next.js must not bundle `worker.js`, or dynamic `import()` of the wasm core breaks.

If this folder is empty, run `npm install`.
