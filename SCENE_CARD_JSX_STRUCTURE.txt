EXACT JSX STRUCTURE — Outermost wrapper down to Copy AI Prompt button
=======================================================================

 1. <main className="min-h-screen ...">                    ← page root (no form, no onClick)
 2.   <div className="max-w-3xl mx-auto">
 3.     ...
 4.     <Tabs value={activeTab} onValueChange={setActiveTab}>   ← Radix Tabs (div)
 5.       <TabsList>...</TabsList>
 6.       <TabsContent value="scenes" className="mt-6 space-y-4">  ← Radix TabsContent (div, role=tabpanel; may have data-state=active/inactive)
 7.         <Card> (Character Setup) ... </Card>
 8.         <div> (Pro tip) ... </div>
 9.         {scenes.map((scene, i) => (
10.           <Card key={i} className="...">              ← div (card.tsx: <div>)
11.             <CardHeader className="pb-2">            ← div (card.tsx: <div>)
12.               <CardTitle className="...">             ← h3 (card.tsx: <h3>)
13.                 <span>Scene {i+1} · ...</span>
14.                 <div className="flex flex-wrap ...">   ← div
15.                   <Select>...</Select>
16.                   <ToggleGroup>...</ToggleGroup>
17.                 </div>
18.               </CardTitle>
19.             </CardHeader>
20.             <CardContent className="space-y-4 ...">   ← div (card.tsx: <div className="p-6 pt-0">)
21.               <div className="flex items-center gap-2 flex-wrap">  ← div (wrapper)
22.                 <button type="button" onClick={...}>   ← Copy AI Prompt
23.                   Copy AI Prompt
24.                 </button>
25.               </div>
26.               ...
27.             </CardContent>
28.           </Card>
29.         ))}
30.       </TabsContent>
31.     </Tabs>
32.   </div>
33. </main>

CHECKLIST:
- No <form> in this tree. Button already has type="button".
- No parent has onClick with e.stopPropagation() in this scene card tree.
- No pointer-events: none in VideoCreationGuide on these elements (TabsContent is Radix; may need override).
- Button has no disabled prop.
