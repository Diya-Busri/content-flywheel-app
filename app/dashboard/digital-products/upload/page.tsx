import UploadProductClient from "./UploadProductClient";

export const metadata = {
  title: "Upload Your Product — Content Flywheel",
};

export default function UploadProductPage() {
  return <UploadProductClient />;
}
